1 任务：将单频段 FIR 基线升级为 4 频段多频段音频处理基线，并修复频谱图直流偏移问题。

2 问题（团队需求）：Tier 2 需要实现多频段处理，需要提供可靠的软件对比基准。调试中发现频谱图 0Hz 处有巨大直流尖峰，掩盖了正常的 440Hz 信号。

3 应用模型：DeepSeek

4 模型回答：AI 建议在 FFT 前加入去直流操作 `x = x - np.mean(x)`，并限制坐标轴范围 `plt.xlim(0, 2000)` 与 `plt.ylim(0, 1000)`。同时指出当前脚本仅读取左声道(440Hz)，导致绘图时两条线完全重叠，看起来只有一条黄色线，滤波效果无法直观展示。

5 收获：成功重写 FIR 与多频段脚本，彻底消除了 0Hz 直流尖峰，输出了干净准确的频谱图。深刻理解了数字信号处理中去均值的重要性。虽然因数据源限制暂时未能直观展示 880Hz 被滤除的效果，但算法逻辑已完全跑通，后续将尝试左右声道混合输入以验证滤波衰减。